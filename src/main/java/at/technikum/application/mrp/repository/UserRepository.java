package at.technikum.application.mrp.repository;

public class UserRepository {

    //klassische Funktionen eines Repositorys:

    //Optional<User> find(String id); -> warum find? weil wir davon ausgehen, dass es den User mit der Id auch nicht geben kann

    //List<User> findAll();

    //User save(User user);

    //User update(User user); - existiert nicht immer

    //User delete(String id);

}
